<Window x:Class="FolMinder2.Presentation.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolMinder2.Presentation"
        xmlns:vms="clr-namespace:FolMinder2.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vms:AboutViewModel}"
        SizeToContent="WidthAndHeight"
        Title="FolMinder2について"
        ResizeMode="NoResize"
        Icon="/Assets/folminder.ico"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Padding" Value="10,5,10,5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5,10,5"/>
            <Setter Property="Margin" Value="10,5,10,5"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10,5,10,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Column="0"
               Grid.RowSpan="4"
               Width="64"
               Source="pack://application:,,/Assets/64.png"
               Stretch="Uniform"/>
        <TextBlock Grid.Row="0"
                   Grid.Column="1"
                   Grid.ColumnSpan="3">FolMinder2のバージョン情報</TextBlock>
        <TextBlock Grid.Row="1"
                   Grid.Column="1"
                   Text="会社名"/>
        <TextBlock Grid.Row="1"
                   Grid.Column="2"
                   Text="{Binding Company}"/>
        <TextBlock Grid.Row="2"
                   Grid.Column="1"
                   Text="製品名"/>
        <TextBlock Grid.Row="2"
                   Grid.Column="2"
                   Text="{Binding Product}"/>
        <TextBlock Grid.Row="3"
                   Grid.Column="1"
                   Text="バージョン"/>
        <TextBlock Grid.Row="3"
                   Grid.Column="2"
                   Text="{Binding Version}"/>
        <TextBlock Grid.Row="4"
                   Grid.Column="0"
                   Grid.ColumnSpan="3"
                   Text="{Binding Copyright}"/>
        <StackPanel Grid.Row="5"
                    Grid.Column="0"
                    Grid.ColumnSpan="3">
            <Button HorizontalAlignment="Right"
                    x:Name="acceptButton"
                    Content="閉じる"
                    IsDefault="True"
                    Click="acceptButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
